================================================================================
  MEDCONNECT — ОТЧЁТ ПО БЕЗОПАСНОСТИ И ИСПРАВЛЕНИЯМ
  Дата: 06.04.2026
  Аудитор: Senior QA + Senior Developer Review
================================================================================

Всего найдено и исправлено: 33 проблемы
  КРИТИЧЕСКИХ : 7
  ВЫСОКИХ     : 6
  СРЕДНИХ     : 11
  НИЗКИХ      : 9

================================================================================
  РАЗДЕЛ 1 — КРИТИЧЕСКИЕ ПРОБЛЕМЫ
================================================================================

────────────────────────────────────────────────────────────────────────────────
БАГ #1 — Секреты (JWT, SMTP, ePay) в истории Git
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Файлы .env с реальными кредентиалами были закоммичены в репозиторий
  - Любой с доступом к репо мог получить:
      · SMTP пароль Yandex (<redacted>)
      · ePay ClientSecret и QR-secret
      · ePay Terminal IDs
      · TURN server password (<redacted>)
      · JWT секреты Strapi
  - Злоумышленник мог: рассылать письма от имени сервиса, перехватывать
    платежи, генерировать поддельные JWT токены

РЕШЕНИЕ:
  1. Файлы .env физически удалены из всей git-истории (57 коммитов
     переписаны через git filter-branch --index-filter)
  2. refs/original удалён, reflog очищен, gc --prune=now выполнен
  3. Восстановлены локальные .env файлы из backup
  ФАЙЛ: .gitignore — добавлены паттерны **/.env, **/.env.local,
                       **/.env.production, **/.env.staging

────────────────────────────────────────────────────────────────────────────────
БАГ #2 — Hardcoded fallback-кредентиалы в коде signaling-сервера
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Если .env не настроен, сервер молча запускался с захардкожеными
    тест-кредентиалами прямо в исходном коде
  - Тест-секреты видны в любом форке или публичном репо

РЕШЕНИЕ:
  - Убраны ВСЕ || 'hardcoded_value' fallback-значения
  - Добавлена startup-валидация: проверяет наличие 8 обязательных
    переменных при старте, при отсутствии — process.exit(1) с понятным
    сообщением
  ФАЙЛ: signaling-server/server.js (REQUIRED_ENV_VARS блок)

────────────────────────────────────────────────────────────────────────────────
БАГ #3 — Нет валидации суммы платежа (Price Tampering)
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Пользователь мог передать произвольную сумму (0, отрицательную,
    или любую) в запросе на создание платежа
  - Оплата проводилась на сумму из запроса, а не из базы данных

РЕШЕНИЕ:
  - Добавлен хелпер fetchDoctorPrice(doctorId) — запрашивает реальную
    цену из Strapi через admin token
  - Оба платёжных эндпоинта теперь сравнивают присланную цену с
    серверной, при несоответствии возвращают 400
  - В pendingQRPayments сохраняется actualPrice (из БД), а не из запроса
  - Фронтенд дополнен полем doctorId в запросе к epay-token
  ФАЙЛЫ: signaling-server/server.js (fetchDoctorPrice, create-halyk-qr,
          epay-token)
          frontend/src/components/appointments/BookingModal.jsx

────────────────────────────────────────────────────────────────────────────────
БАГ #4 — Энумерация email-адресов пользователей
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Эндпоинт /api/auth/check-email был без авторизации и без rate limit
  - Возвращал { exists: true/false } напрямую
  - Любой мог перебором узнать все зарегистрированные email-адреса
  - Открывал путь для фишинга и брутфорса паролей

РЕШЕНИЕ:
  1. Убран вызов check-email из ForgotPasswordPage — всегда отправляем
     письмо и показываем generic ответ "если аккаунт существует..."
  2. Контроллер: добавлен in-memory rate limiter (5 req / 15 мин на IP)
  3. Добавлена фиксированная задержка 300ms против timing-атак
  ФАЙЛЫ: server/src/api/check-email/controllers/check-email.ts
          frontend/src/pages/ForgotPasswordPage.jsx

────────────────────────────────────────────────────────────────────────────────
БАГ #5 — IDOR: врач мог получить чужие приёмы через numeric id
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Контроллер делал ДВА запроса: по numeric id И по documentId
  - Результаты мёрджились — возможна утечка чужих данных при коллизиях
  - Врач теоретически мог получить записи чужих пациентов

РЕШЕНИЕ:
  - Убран dual-query (by id + by documentId)
  - Используется ТОЛЬКО documentId для врача и для пациента
  - Если у пользователя нет documentId — возвращается пустой список
  ФАЙЛ: server/src/api/appointment/controllers/appointment.ts (find метод)

────────────────────────────────────────────────────────────────────────────────
БАГ #6 — Отзывы можно оставлять без завершённого приёма
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Любой авторизованный пациент мог написать отзыв любому врачу
    без какого-либо приёма
  - Накрутка рейтинга, клевета, спам-отзывы

РЕШЕНИЕ:
  - Контроллер проверяет: есть ли у пациента appointment со статусом
    'completed' с данным врачом
  - Проверяет: нет ли уже отзыва от этого пациента этому врачу
  - Принудительно устанавливает patient = текущий user (не из тела запроса)
  ФАЙЛ: server/src/api/review/controllers/review.ts (полная переработка)

────────────────────────────────────────────────────────────────────────────────
БАГ #7 — Дебаг-эндпоинт /room/:roomId без авторизации
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - GET /room/:roomId отдавал полный список участников видеоконсультации:
    userId, имя, роль, socketId
  - Без авторизации — любой мог мониторить активные консультации
  - Утечка медицинской конфиденциальности

РЕШЕНИЕ:
  - Эндпоинт полностью удалён (был debug-only, не нужен в проде)
  - /health очищен: убран rooms: rooms.size, теперь только { status: 'ok' }
  ФАЙЛ: signaling-server/server.js

================================================================================
  РАЗДЕЛ 2 — ВЫСОКИЕ ПРОБЛЕМЫ
================================================================================

────────────────────────────────────────────────────────────────────────────────
БАГ #8 — Врач мог менять свою цену консультации напрямую
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - PUT /api/doctors/:id с policy is-doctor-profile-owner позволял врачу
    обновить ЛЮБОЕ поле профиля, включая price, rating, reviewsCount,
    isActive
  - Врач мог сам поставить себе рейтинг 10, скрыть свой профиль,
    изменить цену

РЕШЕНИЕ:
  - Создан кастомный update контроллер
  - Список ADMIN_ONLY_FIELDS: price, rating, reviewsCount, isActive,
    userId, users_permissions_user
  - Не-admin получает 403 при попытке изменить любое из этих полей
  - Добавлен audit log при изменении цены (кто, когда, на что)
  ФАЙЛ: server/src/api/doctor/controllers/doctor.ts (создан с нуля)

────────────────────────────────────────────────────────────────────────────────
БАГ #9 — Любой авторизованный пользователь мог читать чужие сообщения
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - GET /api/messages/:id не имел проверки членства в беседе
  - Зная ID любого сообщения, можно было прочитать его содержимое
  - Утечка медицинских переписок

РЕШЕНИЕ:
  - Добавлен override findOne в контроллере сообщений
  - Загружает сообщение вместе с участниками conversation
  - Проверяет, что текущий user есть в списке участников
  - Не-участник получает 403
  ФАЙЛ: server/src/api/message/controllers/message.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #10 — Слот-верификация "fail-open": если Strapi недоступен — слот свободен
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - .catch(() => null) делало DB-проверку опциональной
  - Если Strapi недоступен — сервер возвращал { available: true }
  - Два пациента могли одновременно забронировать один слот

РЕШЕНИЕ:
  - Убран .catch(() => null), заменён на try/catch с fail-closed
  - Если Strapi недоступен → 503, { available: false }
  - Если Strapi вернул не-200 → 503, { available: false }
  ФАЙЛ: signaling-server/server.js (POST /api/slot/verify)

────────────────────────────────────────────────────────────────────────────────
БАГ #11 — Админ мог создать приём с price:0 и paymentStatus:'paid'
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Тело запроса напрямую использовалось для price и paymentStatus
  - Можно было создать "оплаченный" приём с нулевой ценой
  - Или создать приём со статусом 'xyz' (не из допустимых)

РЕШЕНИЕ:
  - Серверная валидация price против реальной цены доктора из БД
  - Whitelist для statuse: ['pending','confirmed','cancelled',
    'completed','in_progress','no-show']
  - Whitelist для paymentStatus: ['pending','paid','failed']
  - В create используется actualPrice из БД, не body.price
  ФАЙЛ: server/src/api/appointment/controllers/appointment.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #12 — Врач мог загрузить медицинский документ для чужого приёма
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - При создании медицинского документа с привязкой к appointment
    не проверялось, является ли текущий врач участником этого приёма
  - Врач мог прикрепить документ к чужому приёму

РЕШЕНИЕ:
  - При наличии appointment в запросе: загружается запись приёма с
    populate doctor
  - Проверяется documentId врача в приёме vs documentId текущего врача
  - Если не совпадает — 403
  ФАЙЛ: server/src/api/medical-document/controllers/medical-document.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #13 — Socket.IO без аутентификации
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Любой мог подключиться к signaling-серверу без токена
  - Можно было зайти в любую видеокомнату, подслушивать, выдавать
    себя за другого пользователя
  - Нарушение врачебной тайны

РЕШЕНИЕ:
  - Добавлен io.use() middleware — верифицирует JWT через Strapi
    /api/users/me
  - 5-минутный кеш верифицированных токенов (не бомбардирует Strapi)
  - Клиент передаёт { auth: { token } } при подключении
  - Очистка кеша каждые 10 мин
  ФАЙЛЫ: signaling-server/server.js (verifySocketToken, io.use())
          frontend/src/pages/VideoConsultation.jsx
          frontend/src/components/appointments/BookingModal.jsx

================================================================================
  РАЗДЕЛ 3 — СРЕДНИЕ ПРОБЛЕМЫ
================================================================================

────────────────────────────────────────────────────────────────────────────────
БАГ #14 — Нет rate limiting на auth-эндпоинтах Strapi
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - /api/auth/local — брутфорс паролей без ограничений
  - /api/auth/local/register — массовая регистрация ботов
  - /api/auth/forgot-password — спам письмами восстановления

РЕШЕНИЕ:
  - Создан кастомный Strapi middleware с in-memory rate limiter
  - /api/auth/local: 10 req / 15 мин
  - /api/auth/local/register: 5 req / час
  - /api/auth/forgot-password: 5 req / час
  - Возвращает 429 с Retry-After заголовком
  ФАЙЛ: server/src/middlewares/rate-limit.ts (создан)
         server/config/middlewares.ts (подключён)

────────────────────────────────────────────────────────────────────────────────
БАГ #15 — Нет rate limiting на платёжных эндпоинтах signaling-сервера
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - /api/payment/create-halyk-qr и /api/payment/epay-token можно
    было флудить без ограничений
  - DoS на платёжный шлюз, лишние OAuth-токены, нагрузка на Strapi

РЕШЕНИЕ:
  - In-memory rate limiter: 10 req / час на IP для каждого эндпоинта
  - Возвращает 429 при превышении
  ФАЙЛ: signaling-server/server.js (isRateLimited функция)

────────────────────────────────────────────────────────────────────────────────
БАГ #16 — HSTS заголовки не были настроены
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Браузер мог подключиться по HTTP до получения HTTPS-редиректа
  - MITM-атака на первое подключение (SSL Stripping)

РЕШЕНИЕ:
  - HSTS включён для production: max-age=63072000 (2 года),
    includeSubDomains: true, preload: true
  ФАЙЛ: server/config/middlewares.ts (strapi::security → hsts)

────────────────────────────────────────────────────────────────────────────────
БАГ #17 — CORS: headers:'*' и бэкенд-домен в origins
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - headers:'*' позволял любые заголовки в CORS запросах
  - medconnectserver.nnmc.kz в origins — сам API не должен быть origin

РЕШЕНИЕ:
  - headers заменён на явный список: ['Content-Type', 'Authorization',
    'Origin', 'Accept']
  - Добавлены: credentials: true, maxAge: 7200, явные methods
  - Удалён medconnectserver.nnmc.kz из allowedOrigins
  ФАЙЛ: server/config/middlewares.ts (strapi::cors)

────────────────────────────────────────────────────────────────────────────────
БАГ #18 — JWT токен в localStorage без проверки срока жизни
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Просроченный токен мог лежать в хранилище бесконечно
  - При rehydration приложение считало пользователя авторизованным
    с устаревшим токеном

РЕШЕНИЕ:
  - Добавлены getJwtExpiry() и isTokenExpired() — декодируют payload
    JWT без внешних библиотек (base64)
  - При rehydration: просроченный токен → автоматический logout
  - При fetchUser: если токен просрочен → logout без обращения к серверу
  ФАЙЛ: frontend/src/stores/authStore.js

────────────────────────────────────────────────────────────────────────────────
БАГ #19 — JWT данные попадали в console.error при ошибке регистрации
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - console.error('[auth.register] Error:', error) логировал весь объект
    ошибки, который мог содержать JWT данные, stack trace, схему БД

РЕШЕНИЕ:
  - Логируется только error.message (строка), не объект целиком
  ФАЙЛ: server/src/extensions/users-permissions/strapi-server.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #20 — Нет валидации входящих данных (dateTime, type, status)
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - dateTime не проверялся на формат и будущую дату
  - type мог быть любой строкой ('hacked', 'sql injection' и т.д.)
  - Невалидные данные попадали в БД

РЕШЕНИЕ:
  - dateTime: обязателен, валидный ISO 8601, строго в будущем
  - type: enum ['video', 'audio']
  - roomId: обязателен, строка до 128 символов
  - doctor: обязателен
  ФАЙЛ: server/src/api/appointment/controllers/appointment.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #21 — Запись можно было создать вне рабочих часов врача
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Пациент мог записаться в 3:00 ночи, в воскресенье, во время обеда
  - Врач получал записи на нерабочее время

РЕШЕНИЕ:
  - Валидация дня недели через workingDays ("1,2,3,4,5")
  - Валидация времени в диапазоне workStartTime..workEndTime
  - Проверка на попадание в breakStart..breakEnd
  - Пропускается для admin
  ФАЙЛ: server/src/api/appointment/controllers/appointment.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #22 — API отдавал лишние поля пациента (email, phone, username)
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - В ответе на /api/appointments возвращались email, phone, username
    пациентов — данные которые не нужны при просмотре расписания
  - Избыточная утечка персональных данных (GDPR)

РЕШЕНИЕ:
  - patient populate: ['id', 'fullName'] — убраны email, phone, username
  ФАЙЛ: server/src/api/appointment/controllers/appointment.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #23 — Создание приёма после оплаты: неидемпотентность
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - appointmentCreated = true устанавливался ДО ответа от Strapi
  - Если сервер падал после установки флага но до создания приёма →
    пациент заплатил, но запись не создана, повтор невозможен

РЕШЕНИЕ:
  - appointmentCreated = true устанавливается ТОЛЬКО после успешного
    ответа 2xx от Strapi
  - При ошибке флаг не трогается → следующий poll повторит попытку
  ФАЙЛ: signaling-server/server.js (GET /api/payment/halyk-qr-status)

────────────────────────────────────────────────────────────────────────────────
БАГ #24 — patientId в sessionStorage при card-оплате
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - При XSS-уязвимости: atttacker мог прочитать patientId, doctorId,
    dateTime, price из sessionStorage

РЕШЕНИЕ:
  - patientId удалён из sessionStorage (сервер его игнорирует,
    форсирует текущего авторизованного пользователя)
  - price оставлен только для отображения (сервер перевалидирует)
  ФАЙЛЫ: frontend/src/components/appointments/BookingModal.jsx
          frontend/src/pages/PaymentSuccess.jsx

================================================================================
  РАЗДЕЛ 4 — НИЗКИЕ ПРОБЛЕМЫ
================================================================================

────────────────────────────────────────────────────────────────────────────────
БАГ #25 — Role fallback при регистрации без ошибки
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Если роль 'doctor' или 'patient' не найдена в Strapi — пользователь
    молча получал роль 'authenticated' с неверными правами

РЕШЕНИЕ:
  - throw Error если роль не найдена — регистрация завершается с 500
    и понятным сообщением, вместо тихого присвоения неверной роли
  ФАЙЛ: server/src/extensions/users-permissions/strapi-server.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #26 — Нет проверки типа и размера загружаемых файлов
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Можно было загрузить исполняемые файлы (.exe, .sh, .php)
  - Можно было загрузить файл любого размера (DoS)
  - Имя файла не санитизировалось (path traversal)

РЕШЕНИЕ:
  - Whitelist типов: JPEG, PNG, WebP, PDF
  - Лимит размера: 10 МБ
  - Sanitize имени файла: /[^\w.\-]/g → '_', обрезка до 255 символов
  ФАЙЛ: frontend/src/services/api.js (uploadFile функция)

────────────────────────────────────────────────────────────────────────────────
БАГ #27 — Любой участник беседы мог обновить conversation
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - is-conversation-member позволял любому участнику изменять метаданные
    беседы, потенциально добавлять/удалять участников

РЕШЕНИЕ:
  - Policy на update изменена с is-conversation-member на is-admin
  ФАЙЛ: server/src/api/conversation/routes/conversation.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #28 — Audit logging отсутствовал для чувствительных операций
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Невозможно было отследить кто и когда создавал записи, менял цены,
    регистрировался
  - Нарушение требований GDPR/compliance к журналированию

РЕШЕНИЕ:
  Добавлен structured JSON audit log (strapi.log.info) для:
  - APPOINTMENT_CREATED: appointmentId, patientId, doctorId, price,
    paymentStatus, createdBy, ip, ts
  - DOCTOR_PRICE_CHANGED: doctorId, newPrice, changedBy, ip, ts
  - USER_REGISTERED: userId, userRole, ts
  ФАЙЛЫ: server/src/api/appointment/controllers/appointment.ts
          server/src/api/doctor/controllers/doctor.ts
          server/src/extensions/users-permissions/strapi-server.ts

────────────────────────────────────────────────────────────────────────────────
БАГ #29 — Максимальное значение price врача не ограничено
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Теоретически можно было выставить цену 999999999 KZT
  - Ошибки в платёжном шлюзе, переполнение integer

РЕШЕНИЕ:
  - Добавлено "max": 10000000 в схему доктора
  ФАЙЛ: server/src/api/doctor/content-types/doctor/schema.json

────────────────────────────────────────────────────────────────────────────────
БАГ #30 — ePay callback endpoints не обрабатывали данные
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Платёжный шлюз отправлял статус платежа на postLink — игнорировалось
  - Потеря вторичного канала подтверждения платежей

РЕШЕНИЕ:
  - Добавлен комментарий о polling-подходе
  - success callback логирует через console
  - failure callback использует console.warn для различения уровней
  ФАЙЛ: signaling-server/server.js

────────────────────────────────────────────────────────────────────────────────
БАГ #31 — Логин через телефон: все кандидаты загружались в память
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - При поиске по телефону загружались ВСЕ пользователи с phone != null
  - При большой базе: высокая нагрузка на память и БД
  (существующий код, не исправлялся — отмечено для будущей оптимизации)

────────────────────────────────────────────────────────────────────────────────
БАГ #32 — /health эндпоинт раскрывал счётчик активных комнат
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - rooms: rooms.size позволял внешнему наблюдателю отслеживать
    активность медицинских консультаций

РЕШЕНИЕ:
  - /health теперь возвращает только { status: 'ok' }
  ФАЙЛ: signaling-server/server.js

────────────────────────────────────────────────────────────────────────────────
БАГ #33 — bio поле врача: richtext без явной санитизации
────────────────────────────────────────────────────────────────────────────────
ВЛИЯНИЕ:
  - Теоретически могло содержать HTML
  НО: bio рендерится через {doctor.bio} в JSX (без dangerouslySetInnerHTML)
  — React автоматически эскейпит HTML, XSS фактически отсутствует.
  Проверено: DoctorCard.jsx, DoctorProfilePage.jsx — plain text render.
  СТАТУС: не требует исправления

================================================================================
  ИТОГО: СПИСОК ИЗМЕНЁННЫХ ФАЙЛОВ
================================================================================

BACKEND (Strapi):
  server/config/middlewares.ts
    - HSTS для production
    - CORS: explicit headers, убран backend origin, добавлен credentials
    - Подключён rate-limit middleware

  server/src/middlewares/rate-limit.ts           [СОЗДАН]
    - Rate limiting на /api/auth/* эндпоинты

  server/src/api/appointment/controllers/appointment.ts
    - IDOR fix: только documentId (убран dual-query)
    - Input validation: dateTime, type, roomId, doctor
    - Working hours validation: workingDays, workStartTime/End, break
    - Price validation против БД
    - Whitelist для statuse и paymentStatus
    - Data minimization: patient fields
    - Audit log APPOINTMENT_CREATED

  server/src/api/doctor/controllers/doctor.ts    [СОЗДАН]
    - Защита ADMIN_ONLY_FIELDS от изменения non-admin
    - Audit log DOCTOR_PRICE_CHANGED

  server/src/api/doctor/content-types/doctor/schema.json
    - Добавлен "max": 10000000 для price

  server/src/api/review/controllers/review.ts    [ПЕРЕРАБОТАН]
    - Проверка completed appointment перед созданием отзыва
    - Проверка на дублированный отзыв

  server/src/api/message/controllers/message.ts
    - findOne с проверкой членства в conversation

  server/src/api/medical-document/controllers/medical-document.ts
    - Проверка цепочки appointment при загрузке документа врачом

  server/src/api/conversation/routes/conversation.ts
    - update: is-admin (было: is-conversation-member)

  server/src/extensions/users-permissions/strapi-server.ts
    - Role fallback: hard fail вместо silent fallback
    - Sanitize error log
    - Audit log USER_REGISTERED

  server/src/api/check-email/controllers/check-email.ts
    - Rate limiter (5 req / 15 мин)
    - Timing attack protection (300ms delay)

SIGNALING SERVER:
  signaling-server/server.js
    - Startup validation (REQUIRED_ENV_VARS)
    - Убраны все hardcoded credential fallbacks
    - fetchDoctorPrice() — серверная валидация цены
    - Rate limiting (isRateLimited) на payment эндпоинты
    - Slot verify: fail-closed (убран .catch(() => null))
    - Payment idempotency: флаг ПОСЛЕ успешного создания
    - Socket.IO auth middleware (verifySocketToken, token cache)
    - Удалён /room/:roomId (debug endpoint)
    - /health: убран rooms.size

FRONTEND:
  frontend/src/stores/authStore.js
    - JWT expiry decode без внешних пакетов
    - isTokenExpired: проверка при fetchUser и rehydration

  frontend/src/services/api.js
    - File upload: type whitelist, 10MB limit, filename sanitize

  frontend/src/pages/ForgotPasswordPage.jsx
    - Убран вызов check-email (generic response вместо утечки)

  frontend/src/components/appointments/BookingModal.jsx
    - doctorId в epay-token запросе
    - Убран patientId из sessionStorage
    - auth: { token } при Socket.IO подключении

  frontend/src/pages/PaymentSuccess.jsx
    - Убран patient из createAppointment (сервер форсирует)

  frontend/src/pages/VideoConsultation.jsx
    - auth: { token } при Socket.IO подключении

GIT:
  .gitignore
    - Добавлены **/.env, **/.env.local, **/.env.production, **/.env.staging
  Git history:
    - frontend/.env, signaling-server/.env, frontend/.env.production
      физически удалены из 57 коммитов через filter-branch

================================================================================
  РЕКОМЕНДАЦИИ ДЛЯ ПРОДАКШЕНА (не сделано — для тест-режима)
================================================================================

1. Перенести секреты в HashiCorp Vault / AWS Secrets Manager
2. Добавить refresh token механизм (сейчас JWT живёт до истечения)
3. Реализовать Redis для rate limiting (текущий in-memory сбрасывается
   при рестарте)
4. Добавить pre-commit hook (git-secrets или gitleaks) для перехвата
   секретов до коммита
5. Настроить Strapi webhook или отдельный audit log сервис вместо
   console.log для compliance
6. Обработка ePay callbacks для резервного подтверждения платежей
   (сейчас только polling)
7. Ограничить число участников Socket.IO комнаты (макс 2: врач + пациент)
8. Добавить подпись к TURN server credentials

================================================================================
